import time
import serial
try:
	ser = serial.Serial('/dev/ttyACM0' , 9600, timeout=2)
except :
	ser = serial.Serial('/dev/ttyACM1' , 9600, timeout=2)

bool = true
while(bool):
	int i +=
	if i = 100:
		bool = false
	ser.read(9)
	sleep(5)
